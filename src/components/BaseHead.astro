---
// Import the global.css file here so that it is included on
// all pages through the use of the <BaseHead /> component.
import type { ImageMetadata } from 'astro';
import FallbackImage from '../assets/img/home/fondo-productos-panaderia-dorita.jpg';
import { SITE_TITLE } from '../consts';
import { Font } from 'astro:assets';
import '../styles/global.css';

interface Props {
  title: string;
  description: string;
  image?: ImageMetadata;
  structuredData?: object[];
  robots?: string;
  preloadImage?: boolean; // Prop para controlar la precarga
}

const canonicalURL = new URL(Astro.url.pathname, Astro.site);

const { 
  title, 
  description, 
  image = FallbackImage, 
  structuredData = [], 
  robots = "index, follow",
  preloadImage = true 
} = Astro.props;

// Capturamos la ruta actual
const pathname = Astro.url.pathname;

// Definimos únicamente las rutas exactas de las categorías principales
const exactCategoryPages = ['/panaderia', '/panaderia/', '/pasteleria', '/pasteleria/'];

// Comprobamos si la página actual es EXACTAMENTE una de ellas
const isMainCategoryPage = exactCategoryPages.includes(pathname);

// Solo precarga si preloadImage es true Y NO es la portada de categoría
const shouldPreload = preloadImage && !isMainCategoryPage;

---

<!-- Global Metadata -->
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<link rel="shortcut icon" href="/favicon.ico" />
<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
<link rel="manifest" href="/site.webmanifest" />
<link rel="sitemap" href="/sitemap-index.xml" />
<link
  rel="alternate"
  type="application/rss+xml"
  title={SITE_TITLE}
  href={new URL('rss.xml', Astro.site)}
/>
<meta name="generator" content={Astro.generator} />

<!-- Preload LCP image (Solo si no es página de categoría) -->
{shouldPreload && (
  <link rel="preload" as="image" href={image.src} fetchpriority="high" />
)}

<!-- Canonical URL -->
<link rel="canonical" href={canonicalURL} />
<meta name="robots" content={robots} />

<Font cssVariable="--font-display" preload />
<Font cssVariable="--font-cuerpo" preload />

<!-- Primary Meta Tags -->
<title>{title}</title>
<meta name="description" content={description} />

<!-- Open Graph / Facebook -->
<meta property="og:type" content="website" />
<meta property="og:url" content={Astro.url} />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:image" content={new URL(image.src, Astro.url)} />

<!-- Twitter -->
<meta name="twitter:card" content="summary_large_image" />

<!-- Structured Data (JSON-LD) -->
{structuredData.map((schema) => (
  <script type="application/ld+json" set:html={JSON.stringify(schema)} />
))}

<script is:inline>
  window.dataLayer = window.dataLayer || [];

  function gtag() {
    dataLayer.push(arguments);
  }

  window.gtag = gtag;

  // Consentimiento inicial
  gtag('consent', 'default', {
    ad_storage: 'denied',
    ad_user_data: 'denied',
    ad_personalization: 'denied',
    analytics_storage: 'denied',
    wait_for_update: 2000
  });

  // ==========================================
  // CARGAR GOOGLE ANALYTICS DESPUÉS DE 2 SEGUNDOS
  // ==========================================

  window.addEventListener('load', function () {
    setTimeout(function () {

      if (document.querySelector(
        'script[src*="googletagmanager.com/gtag/js?id=G-WYPE6Z1LDP"]'
      )) {
        return;
      }

      var gtagScript = document.createElement('script');

      gtagScript.src =
        'https://www.googletagmanager.com/gtag/js?id=G-WYPE6Z1LDP';

      gtagScript.async = true;

      document.head.appendChild(gtagScript);

      gtagScript.onload = function () {
        gtag('js', new Date());
        gtag('config', 'G-WYPE6Z1LDP');
      };

    }, 2000);
  });
</script>